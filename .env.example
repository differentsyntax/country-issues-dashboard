# Optional. Enables live Air Quality data. Get a free token at
# https://aqicn.org/data-platform/token/
# Without it, everything else works normally — Air Quality just shows as
# "not configured" instead of live data.
AQICN_TOKEN=
